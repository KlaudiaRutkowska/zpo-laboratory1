package exercise4;

public interface FlyingBehaviour {
    //we can add "abstract" word in the beginning but we don't heve to since
    //abstraction here is because method doesn't have any body
    abstract void fly();
}
